<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yumnaasim.devfest">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".OrganizationDetail" />
        <activity
            android:name=".OrganizationList"
            android:label="Organizations" />
        <activity
            android:name=".BillingInfo"
            android:label="Billing Info" />
        <activity
            android:name=".Thanks"
            android:label="Thanks" />
        <activity
            android:name=".SignIn"
            android:label="Sign In" />
        <activity
            android:name=".Account"
            android:label="Account" />
        <activity
            android:name=".TabActivity"
            android:label="@string/title_activity_tab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Events"></activity>
    </application>

</manifest>